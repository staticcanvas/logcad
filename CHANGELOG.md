# Changelog

## **logcad** v0.4.19

- Complete build release and auto publish

## **logcad** v0.4.18

- UPDATE: Build now outputs proper ***UDM(browser)*** exports

## **logcad** v0.4.17

- UPDATED: Npm documenation

## **logcad** v0.4.10

- UPDATE: build now outputs proper ***UDM(browser)*** exports and ***ESM modules*** exports.
- FIXED: CDN URL link `jsdelivr` and `unpkg`
 
## **logcad** v0.4.9

- ADDED: named exports for umd(browser) builds
- ADDED: Bannertext to all outputs minified and beautified

## **logcad** v0.4.3

- CHANGE: Switch to compiler `vite` and use `rollup` custom config
- UPDATE: Update/Fixed **JSDoc** documentation

## **logcad** v0.4.2

- Add `logdrl()` to read from local storage log buffer.

## **logcad** v0.4.1

- ADDED: `logc()`, `logd()` to export scope and attached to `window` if source file is loaded in browser
- Removed `UMD` factory wrapper as target is not node environment but browser

## **logcad** v0.2.1

- Publised to NPM and automatically available on jsDelivr
- Provided in `UMD` Universal Module Definition wrapper
